{% extends 'base.html.twig' %}

{% block title %}Mariage{% endblock %}

{% block body %}
    <h1>Mariage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mariage.id }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ mariage.cin }}</td>
            </tr>
            <tr>
                <th>Fichier_retour</th>
                <td>{{ mariage.fichierRetour }}</td>
            </tr>
            <tr>
                <th>DateDemande</th>
                <td>{{ mariage.dateDemande ? mariage.dateDemande|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateRetour</th>
                <td>{{ mariage.dateRetour ? mariage.dateRetour|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Confirmation</th>
                <td>{{ mariage.confirmation ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mariage_index') }}">back to list</a>

    <a href="{{ path('app_mariage_edit', {'id': mariage.id}) }}">edit</a>

    {{ include('mariage/_delete_form.html.twig') }}
{% endblock %}
