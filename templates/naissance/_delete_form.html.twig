<form method="post" action="{{ path('app_naissance_delete', {'id': naissance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ naissance.id) }}">
    <button class="btn">Delete</button>
</form>
