<form method="post" action="{{ path('app_livret_delete', {'id': livret.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livret.id) }}">
    <button class="btn">Delete</button>
</form>
